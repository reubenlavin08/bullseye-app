# Treat installers as binary so git doesn't try to diff/merge them.
# Keeps `git status` and `git log -p` outputs clean.
*.exe binary
*.dmg binary
*.pkg binary
*.zip binary
*.tar.gz binary
*.ico binary
*.png binary -text
*.jpg binary -text
