# --- Core scraping + scheduling (carried over from deal_finder) -------------
curl_cffi>=0.7.0
APScheduler>=3.10
psycopg2-binary>=2.9   # only for migration-from-postgres helper; remove post-launch
requests>=2.31

# --- Desktop shell ----------------------------------------------------------
pywebview>=5.0
pystray>=0.19
plyer>=2.1
keyring>=24.0
Pillow>=10.0           # required by pystray for tray icons

# --- Cloud client + crash reporting -----------------------------------------
sentry-sdk>=1.40

# --- Web UI -----------------------------------------------------------------
Flask>=3.0
Jinja2>=3.1

# --- Dev / test -------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0

# --- Build (only needed locally to make the installer) ----------------------
pyinstaller>=6.0
